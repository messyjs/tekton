# Static Site

A minimal HTML5 static site with CSS reset and basic layout.

## Structure

- `index.html` — Semantic HTML5 boilerplate
- `css/style.css` — Reset + layout styles
- `js/script.js` — Empty with DOMContentLoaded listener

## Development

All source files should use the `.beta` suffix during development.